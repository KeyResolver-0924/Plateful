# Firebase Configuration
# Get these values from Firebase Console → Project Settings → Your Apps → Web App
# --- Firebase Configuration (Expo) ---
# Use the EXPO_PUBLIC_ prefix for all Firebase variables
EXPO_PUBLIC_FIREBASE_API_KEY=your-api-key-here
EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
EXPO_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
EXPO_PUBLIC_FIREBASE_APP_ID=your-app-id

# App Configuration
EXPO_PUBLIC_APP_NAME=Plateful
EXPO_PUBLIC_APP_VERSION=1.0.0

# Development Settings
NODE_ENV=development
EXPO_OS=ios

# API Keys (if you plan to use external services)
# NUTRITION_API_KEY=your-nutrition-api-key
# IMAGE_RECOGNITION_API_KEY=your-image-recognition-api-key

# --- Android Keystore Signing ---
# Path to your .jks keystore file (relative to project root or absolute)
ANDROID_KEYSTORE_PATH=./keystores/my-release-key.jks
# Keystore password
ANDROID_KEYSTORE_PASSWORD=your_keystore_password
# Key alias (the name of the key inside the keystore)
ANDROID_KEY_ALIAS=your_key_alias
# Key password (can be same as keystore password)
ANDROID_KEY_PASSWORD=your_key_password

# Instructions:
# 1. Copy this file to .env
# 2. Replace the placeholder values with your actual Firebase credentials
# 3. Never commit the .env file to version control 